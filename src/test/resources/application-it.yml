spring:
  jpa:
    database: H2
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    platform: H2
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=TRUE;MODE=PostgreSQL
    username: fake_user
    password: fake_password
    driver-class-name: org.h2.Driver